#include <stdio.h>
#include <locale.h>
#include <stdlib.h>
#define SEMANA 7
int main()
{
	setlocale(LC_ALL, "spanish");

	float total, lun, mar, mie, jue, vie, sab, dom;
	int i,x[10], ganancia[10],suma;

   printf("Ingrese las ventas en la semana\n");

   for(i=0; i<=7; i++){
   	printf("dia %d \n", i);
   	scanf("%d", &ganancia[suma]);

   	printf("--------\n");
   }

   printf("------------\n");
   for (suma = 0; suma <=7; ++i)
   {
   	total+=ganancia[suma];
   	printf("%d\n", ganancia[suma]);
   }
   printf("------------\n");
   printf("%.2f\n", total);
	



	getch();
	return 0;
}
